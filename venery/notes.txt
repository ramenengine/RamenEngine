generic collections

array

string

nodetree

dynamic array

dynamic string

map ? 







features
- same words work on different collections
- optional bounds checking




questions/ideas:
- ENOUGH could be called DONE ... 
- should slice/split take destinations and what order should it be in?
- mixing different
- expose the struct words so it's more of a complete package ... add reflection and ability to create custom field definers
